import XCTest
@testable import upgraded_chainsaw

final class upgraded_chainsawTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(upgraded_chainsaw().text, "Hello, World!")
    }
}
